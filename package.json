{
  "name": "entre-amigos",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio público de Entre Amigos + panel de administración bajo /admin",
  "type": "module",
  "scripts": {
    "build:admin": "npm --prefix admin install && npm --prefix admin run build",
    "build": "npm run build:admin && node scripts/build.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
